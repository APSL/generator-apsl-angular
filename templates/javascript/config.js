/**
 * Main file for the <%= moduleNamePascalCase %> module. Its responsible of creation and configuration the module.
 *
 */
angular.module('<%= moduleNamePascalCase %>', []).
  config(function() {

  }).
  run(function() {

  });
